# Skills

Install these skills with the [`skills`](https://skills.sh/) CLI:

```sh
npx skills add primeapple/skills
```

Install globally for all projects:

```sh
npx skills add primeapple/skills -g
```

Install only the `easy-wins` skill:

```sh
npx skills add primeapple/skills --skill easy-wins
```
